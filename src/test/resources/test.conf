app: {
  name: whatsit-test
  port: 0
}

echo: {
  test: "/echo/test",
  test2: {
    path: "/test"
  }
}

cache: {
  enable: true,
  autoCreation: true,
  custom: [
    {
      name: cache1
      enable: true,
      expireTime: 1,
      maxSize: 100,
      initSize: 10
    }
  ]
}

ws: {
  path1: "/v1"
}
